# Temple OS Runtime

This folder stores local Temple AI Studio operating-system runtime data.

Generated subfolders are ignored by Git:

- `state/`
- `logs/`
- `backups/`
- `support/`
- `tmp/`

These files are local operational data and should not contain committed product media, customer-sensitive output, tokens or private project data.
